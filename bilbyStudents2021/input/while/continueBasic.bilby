main{
	mut count := 0;
	mut count2 := 0;
	while (count < 5) {
		count := count +1;
		print "outer" $s count $n;
		
		if(count ==3){
				print "count is" $s count $n;
				continue;
		}	
		print "after continue" $n;
		
		
	}
}